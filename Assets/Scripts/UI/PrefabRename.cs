using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrefabRename : MonoBehaviour
{
    // Start is called before the first frame update
    void Awake()
    {
        gameObject.name = "Dungeon";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
